
services:
  audion-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${AUDION_PORT}:${AUDION_PORT}"
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:${AUDION_PORT}/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
